# Profile image

The portfolio currently uses `public/assets/profile/profile.png`.

- Current image: 1024 × 1365 px
- CSS uses the same `1024 / 1365` aspect ratio without cropping.
- The UI automatically falls back to `profile-placeholder.svg` if the image is missing.
